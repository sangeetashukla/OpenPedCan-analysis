name: "Run OpenPedCan Analysis"
description: "Run analysis workflow"
runs:
  using: "docker"
  image: "pgc-images.sbgenomics.com/d3b-bixu/open-pedcan:${{ github.sha }}"
  entrypoint: "scripts/run-analysis.sh"
